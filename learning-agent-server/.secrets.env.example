# Copy this file to .secrets.env and replace the placeholder.
# Never commit .secrets.env. The backend reads this value and injects it into
# the isolated Codex runtime; the browser never receives the key.
DEEPSEEK_API_KEY=replace_with_your_deepseek_api_key
