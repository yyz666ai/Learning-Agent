# Cobra（命令行 CLI 框架）

## 是什么
Go 写命令行工具的事实标准框架，支持子命令、flag、自动补全。学它 = 学会写一个专业的 CLI。

## 前置
- Go：函数、struct、package（`go.functions`、`go.structs-methods`、`go.packages-modules`）

## 学什么（路线骨架）
1. 根命令与子命令
2. flag 与参数
3. 命令组织与项目结构
4. 一个完整小 CLI（结合 `go.testing-debugging` 写测试）

## 原子
见 `atoms/`（初始为空，边教边沉淀）。
