# 人工复核（待进行）

Owner：项目维护者／用户。当前没有签署人审结果，有效人审分母为0。AI实施、AI代码审查和自动测试均不代替Owner批准。

打开报告后可筛选用例，点击标题看输入、实际回复、GT、失败记录和复测。每个用例可填写复核意见，保存到当前浏览器本机草稿，也可导出JSON。草稿不覆盖原始证据、不改变机器判定，不代表Gate通过。文件模式下某些浏览器禁用localStorage时，请直接导出草稿。

优先复核：

1. 原生 Windows 安装与启动：包含中文/空格路径，实际Codex/DeepSeek诊断，刷新/重试以及关闭窗口后子进程行为。
2. 在 WorkBuddy 预览和系统浏览器中分别执行同一慢诊断，记录请求和代理日志；不要仅凭92%文案推断服务器活着。
3. 报告桌面/窄屏布局、键盘焦点、筛选、下载和本机草稿。
4. 真实课程全链路与教学质量；一次结构校验通过不代表小白一定学会。

争议：若实际结果与GT不一致，保留原run，记录复核理由，再创建新run；不得把未知直接改PASS。
