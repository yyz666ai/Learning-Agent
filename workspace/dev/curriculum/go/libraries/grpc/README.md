# gRPC（远程过程调用）

## 是什么
用 protobuf 定义接口、跨语言高性能通信的框架。学它 = 学会「服务之间怎么按契约通信」。

## 前置
- Go：interface、error、context（`go.interfaces`、`go.context`）

## 学什么（路线骨架）
1. protobuf 定义与代码生成
2. 服务端 / 客户端
3. 流式（server/client/bidirectional）
4. 拦截器与超时

## 原子
见 `atoms/`（初始为空，边教边沉淀）。
