---
name: learner-onboarding
description: Use when 用户首次进来、学习目标或可投入时间变了、给了目标代码仓库，或想重新评估基础时。
---

# 学习者建档（先摸情况，再开讲）

只补齐真正缺失的建档信息。若界面已经确认画像，立刻退出本 Skill，不能再用聊天重复摸底。

## 界面确认快速通道

当请求含 `forbid_more_onboarding=true`，或画像标记为界面已确认并且 `active_plan` 可读取时：

1. 不再询问、复述或要求用户确认基础、主题、时间与偏好；
2. 不重新写画像，不把界面选择降级成 `unknown`；
3. 直接交给教学 Skill，从活动计划的当前任务开始第一小步。

## 执行流程

1. 只读 `$USER_DIR/learning-state.json` 和 `$USER_DIR/profile.md`；用户给了项目路径再读项目链接，别扫历史。
2. **输入框就是新主题入口**。用户直接打字或语音说新内容，先使用 `learning-intent-router` 获得经多轮 slot filling 确认的意图槽位；不先要求他点“学新知识”。
3. 已填的主题、目标、结果、基础证据、期限和范围直接复用，不重复询问；只有缺少会改变路线的信息才问。
4. 用户的主题可以是语言、库、API、工程、项目、面试方向或单一概念；保留他真正说的主题，不套成默认 Go/Python。
5. 用户自己说的是 `self_reported`，小诊断的结果是 `diagnostic_evidence`，别混成「已掌握」。
6. 情况问够了，用一两句话复述你听到的，对方点头才算确认。没确认不写状态。
7. 确认后，按 `assets/profile-template.md` 更新画像，按 `references/state-contract.md` 写状态。
8. 最后只标记「该定学习计划了」，交给 `learning-plan`，别自己开始上课。

## Plan 确认交互

- Plan 准备好后作为对话中的普通 Agent 消息展示，不打开另一个文档框或学习页。
- 审阅 Plan 时收起之前的选择题，**只保留一个紧凑的确认按钮**。
- 不提供“前面快一点”“多做项目”“我想调整”等预设选项。用户要改时直接在输入框说，立即路由 `plan-revision`。

## 概念速学分支

当用户输入“RAG 是什么”“幂等是什么”这类**显式概念问句**，直接改走 `concept_clarity`，不再追问学习路线：

1. 只问一个会改变教法的点击题：“你只想理解概念，还要看代码实现？”
2. 选“只理解概念”记为 `meaning_only`；不问基础，不问每日时长，不做诊断，直接交给短 Plan。
3. 选“还要看代码实现”记为 `code_walkthrough`；只再问一次代码阅读熟悉度，然后立即开始。仍然不问每日时长，不加 3–4 道起点诊断。
4. 不要因为界面有“时长”字段就反向追问。运行时默认值只用于超时与页数上限，不属于用户课表。
5. “这个是什么”“这一页怎么理解”类指代问句是当前课程答疑，留在对话中，不新建学习项目。

## 边界

- 「看看我的进度」→ 走 `learning-progress`，不是重新建档。
- 目标代码默认只读；不读 `.env`、凭据、构建产物。
- 不知道的就记 `unknown`，别猜对方水平、时间。
- 写失败就说失败，别假装保存了。
